[project]
name = "openbb-company-research-tool"
version = "2.0.0"
description = "A bilingual company research radar with benchmark comparison, interactive charts, sanity checks, and category-aware scoring."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Cruise Cao" }]
dependencies = [
    "openbb",
    "openbb-yfinance",
    "yfinance",
    "pandas",
    "matplotlib",
    "plotly",
]
