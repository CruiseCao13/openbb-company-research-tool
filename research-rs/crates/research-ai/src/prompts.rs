pub const PROMPT_VERSION: &str = "v5.0-local-compact-analyst";

pub fn compact_payload_guidance() -> &'static str {
    "Use locked provider facts. Do not invent segment data, target prices, recommendations, or unsupported claims."
}
